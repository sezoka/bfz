const std = @import("std");
const vm = @import("vm.zig");

pub fn optimize(ops: std.ArrayList(vm.Op)) void {
    // const stage_count = 2;
    // const pass_count = 10;
    // const writer = std.io.getStdOut().writer();
    _ = ops;

    // for (0..stage_count) |stage| {
    //     for (0..) |pass| {
    //         var pass_effective = false;

    //     }
    // }
}
